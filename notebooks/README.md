# Notebooks

A notebook is not required to run this portfolio project.

You can optionally create a demonstration notebook that imports the modules from `src/` and runs them against `sample_data/synthetic_events.json`.

Keeping the core logic in Python modules makes the repository easier to review and reuse.
